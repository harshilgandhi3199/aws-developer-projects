// Common constants used across different mini-projects

const AWS_REGION = 'us-east-1';
const S3_BUCKET_NAME = 'your-s3-bucket-name';
const DYNAMODB_TABLE_NAME = 'your-dynamodb-table-name';
const LAMBDA_FUNCTION_NAME = 'your-lambda-function-name';

module.exports = {
    AWS_REGION,
    S3_BUCKET_NAME,
    DYNAMODB_TABLE_NAME,
    LAMBDA_FUNCTION_NAME
};